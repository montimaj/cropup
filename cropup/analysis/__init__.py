"""Orchestrators composed from the geo/ leaves.

May import ``cropup.geo`` and ``cropup.evidence``. Each orchestrator returns a
``Ledger`` so that what it could not measure travels with what it could.
"""
